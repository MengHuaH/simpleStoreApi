# Redis配置
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_PASSWORD=
REDIS_DB=0
REDIS_KEY_PREFIX=simple-store:

# 缓存TTL配置（单位：秒）
CACHE_DEFAULT_TTL=3600
CACHE_SHORT_TTL=300
CACHE_LONG_TTL=86400

# 数据库配置（如果已有，请保持原样）
DB_HOST=localhost
DB_PORT=3306
DB_USERNAME=root
DB_PASSWORD=password
DB_DATABASE=simple_store

# JWT配置（如果已有，请保持原样）
JWT_SECRET=your-jwt-secret
JWT_EXPIRES_IN=3600